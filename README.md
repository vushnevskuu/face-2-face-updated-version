face2face

Настройка отправки формы:

1. Создайте файл `.env.local` в корне проекта.
2. Добавьте строку:

```
VITE_WEB3FORMS_TOKEN=ВАШ_ТОКЕН
```

Токен можно получить в Web3Forms. Приложение читает его из `import.meta.env.VITE_WEB3FORMS_TOKEN`.
